language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-frontendjetpack' ]; then cd .. && eval "mv $currentfolder generator-frontendjetpack" && cd generator-frontendjetpack; fi

